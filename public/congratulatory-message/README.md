# Section 05 — Congratulatory Message

Interactive birthday message / book experience.

## Standalone test
Open `index.html` or serve this folder with a local web server.

## Integration
Copy this section into the master Birthday Journey project only after standalone testing.
Do not import this section's internal JavaScript into `src/main.js` unless its connector is specifically designed for that purpose.
